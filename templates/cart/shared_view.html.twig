{% extends 'base.html.twig' %}

{% block title %}Panier partagé{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-4">Panier partagé</h1>
    {% if cart.items|length > 0 %}
        <ul class="mb-6">
            {% for item in cart.items %}
                <li class="mb-2">
                    {{ item.product.name }} x {{ item.quantity }} ({{ item.getSubtotal()|number_format(2, '.', ' ') }} €)
                </li>
            {% endfor %}
        </ul>
        <div class="font-bold mb-4">Total : {{ cart.getTotal()|number_format(2, '.', ' ') }} €</div>
        {% if app.user %}
            <form method="post" action="{{ path('app_cart_import_shared', {token: cart.shareToken}) }}">
                <button type="submit" class="px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700">Importer ce panier</button>
            </form>
        {% endif %}
    {% else %}
        <div class="text-gray-500">Ce panier est vide.</div>
    {% endif %}
    <a href="/" class="inline-block mt-4 px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">Retour à l'accueil</a>
</div>
{% endblock %}
